# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# App
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Upstash Redis (Phase 7.5a — rate limiting + test_post daily counter)
# When unset, rate-limit and test_post quota are skipped (fail-open) so dev
# environments work without external dependencies.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
